
#

package Data::Store::Consistent::Schema;
use Data::Store::Consistent::Tree;


sub is_valid { #  $schema --> ?
    my ($schema ) = @_;

}

sub build_data_tree_from_schema {
    my ($schema ) = @_;
    my $tree;
    $tree:
}

1;
